{
  "name": "pr-sentinel",
  "version": "1.0.0",
  "description": "AI &amp; Rules Based PR Reviewer - Enforces PR quality rules and auto-generates AI summaries",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "ncc build src/index.ts -o dist --license licenses.txt",
    "all": "npm run build && npm run package",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^2.0.1",
    "@actions/github": "^6.0.1",
    "@google/genai": "^1.34.0",
    "@google/generative-ai": "^0.24.1"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@vercel/ncc": "^0.38.4",
    "dotenv": "^17.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
